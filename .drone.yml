kind: pipeline
type: docker
name: go-tests

steps:
- name: tests
  image: golang
  commands:
  - go install .
  - go test -v ./...

---

kind: pipeline
type: docker
name: js-unit-tests

steps:
- name: jest
  image: node
  commands:
  - yarn install
  - yarn test

---

kind: pipeline
type: docker
name: js-lint

steps:
- name: eslint
  image: node
  commands:
  - yarn install
  - yarn lint

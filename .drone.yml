kind: pipeline
type: docker
name: default

steps:
- name: tests
  image: node
  commands:
  - yarn install
  - yarn test

- name: lint
  image: node
  commands:
  - yarn install
  - yarn lint

- name: complete
  image: node
  commands:
  - echo "DONE!"
  depends_on:
  - tests
  - lint
